import { FormValidationCheck } from './form-validation-check';

describe('FormValidationCheck', () => {
  it('should create an instance', () => {
    expect(new FormValidationCheck()).toBeTruthy();
  });
});
